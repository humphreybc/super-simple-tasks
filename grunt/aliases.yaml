default:
  description: 'Default (production) build'
  tasks:
    - build
dev:
  description: 'Development build'
  tasks:
    - 'build'
    - 'watch'
build:
  description: 'Production build'
  tasks:
    - 'clean'
    - 'coffeelint'
    - 'coffee'
    - 'uglify'
    - 'stylus'
    - 'concat'
    - 'copy'
    - 'compress'
release:
  description: 'Bump version number and build'
  tasks:
    - 'bump'
    - 'build'
    - 'cacheBust'
    - 'manifest'
    - 'compress'